using System.Windows.Media;

namespace Life
{
    public class Picture
    {
        public static void Draw(int[,] gen)
        {
            /*Bitmap b = new Bitmap(200, 100);
            using (Graphics g = Graphics.FromImage(b))
            {
                g.Clear(Color.White);
                // что нибудь рисуете
                // g.DrawRectangle(...
            }*/
        }
    }
}